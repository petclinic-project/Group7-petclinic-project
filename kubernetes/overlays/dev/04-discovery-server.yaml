apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-dev-discovery-server
  namespace: petclinic-dev
  labels:
    app: petclinic-dev-discovery-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petclinic-dev-discovery-server
  template:
    metadata:
      labels:
        app: petclinic-dev-discovery-server
    spec:
      containers:
        - name: discovery-server
          image: 527582557964.dkr.ecr.us-east-1.amazonaws.com/petclinic-dev-discovery-server:19cdf54
          ports:
            - containerPort: 8761
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "384Mi"
              cpu: "200m"
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: petclinic-config
                  key: SPRING_PROFILES_ACTIVE
            - name: SPRING_CLOUD_CONFIG_URI
              valueFrom:
                configMapKeyRef:
                  name: petclinic-config
                  key: SPRING_CLOUD_CONFIG_URI
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8761
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: petclinic-dev-discovery-server
  namespace: petclinic-dev
spec:
  selector:
    app: petclinic-dev-discovery-server
  ports:
    - protocol: TCP
      port: 8761
      targetPort: 8761
